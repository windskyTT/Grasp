`--video_length 10` 既不是 10 秒，也不是 10 次抓取。

它表示录制大约 **10 个环境步对应的画面帧**。实际视频时长取决于保存视频使用的 FPS：

```text
视频时长（秒）≈ 录制帧数 ÷ FPS
```

例如按 30 FPS 保存，10 帧约为：

```text
10 ÷ 30 ≈ 0.33 秒
```

如果希望录制一次完整抓取，你当前设置了 `--episode_length 50`，可以先使用：

```bash
--video_length 50
```

如果希望录制完整的 10 次抓取，则大约使用：

```bash
--video_length 500
```

但当前 PPO 测试会运行 10 轮，每轮 50 个环境步；是否完整覆盖每轮边界还受 `RecordVideo` 的触发方式影响。